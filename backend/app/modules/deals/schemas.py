# Pydantic schemas for HTTP request/response validation (deals module).
# Define request bodies and response shapes here once the Deal ORM model is finalized.
# These schemas are INTERNAL to this module — never imported by other modules.
