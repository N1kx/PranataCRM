# Pydantic schemas for HTTP request/response validation (auth module).
# Define request bodies and response shapes here once the auth domain model is finalized.
# These schemas are INTERNAL to this module — never imported by other modules.
