# Pydantic schemas for HTTP request/response validation (contacts module).
# Define request bodies and response shapes here once the Contact ORM model is finalized.
# These schemas are INTERNAL to this module — never imported by other modules.
