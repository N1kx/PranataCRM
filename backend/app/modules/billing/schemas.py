# Pydantic schemas for HTTP request/response validation (billing module).
# Define request bodies and response shapes here once billing requirements are finalized.
# These schemas are INTERNAL to this module — never imported by other modules.
