# DealContractProtocol — the interface other modules use to access deal data.
# DealDTO — the frozen dataclass that crosses module boundaries (no ORM models or internal schemas).
# Define both once the Deal domain model and its required cross-module operations are agreed upon.
