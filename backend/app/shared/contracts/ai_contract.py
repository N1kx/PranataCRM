# AIContractProtocol — the interface other modules use to request AI capabilities.
# DTOs (e.g. AISummaryDTO, DealScoreDTO) — frozen dataclasses that cross module boundaries.
# Define both once the AI feature set and its cross-module surface area are agreed upon.
