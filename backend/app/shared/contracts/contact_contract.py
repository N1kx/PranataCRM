# ContactContractProtocol — the interface other modules use to access contact data.
# ContactDTO — the frozen dataclass that crosses module boundaries (no ORM models or internal schemas).
# Define both once the Contact domain model and its required cross-module operations are agreed upon.
