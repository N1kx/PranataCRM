# NotificationContractProtocol — the interface other modules use to trigger notifications.
# NotificationDTO — the frozen dataclass that crosses module boundaries.
# Define both once the notification channels and delivery requirements are agreed upon.
